
#### Prio 1 — Release-Blocker (Kunde kann nicht testen)

|#|Thema|Quelle|
|---|---|---|
|**B1**|**Mac-Build nicht startfähig** — „PaperTrail Compare.app ist beschädigt". Aus Terminal gestartet funktioniert es. → Typisch: fehlende Code-Signatur / fehlende Notarization / defektes Bundle.|Dein Screenshot|
|**B2**|**Windows Einzelvergleich: JSON-Parse-Fehler** — `expected value at line 1 column 1`. Vergleich läuft durch, Report wird erzeugt, aber Tauri kann die Engine-Antwort nicht parsen. → Vermutlich die `fitz`-Deprecation-Warning (B3) verschmutzt stdout.|Dein Punkt 2|
|**B3**|**`fitz`-Deprecation-Warning auf stdout** — `import fitz` → Warning auf stdout → JSON-Output wird korrumpiert → B2.|Dein Punkt 1|
|**B4**|**`exclude_regions` nicht in Produktionspipeline verdrahtet** — `region_filter.extract_pages_excluding_regions` wird weder in `__main__.py` noch in `batch_processor.py` aufgerufen. Feature existiert, wirkt aber nie.|Bekannter Bug|

#### Prio 2 — Funktionale Bugs (Falschergebnisse / UX-Probleme)

|#|Thema|Quelle|
|---|---|---|
|**B5**|**`normalize_whitespace` unwirksam** — 1000+ falsche Whitespace-Deltas trotz aktivem Toggle. GUI→Engine-Durchreichung kaputt.|Bekannter Bug|
|**B6**|**Report-PDF ~400 MB** — aus 2×500 KB Quellen. Unkomprimierte Pixmaps in Side-by-Side-Layout.|Bekannter Bug|
|**B7**|**OCR-Indikator zeigt „Nein"** trotz Image-PDF. Entweder `ocr.enabled` nicht durchgereicht oder Scanner-OCR-Layer maskiert als Text.|Bekannter Bug|
|**B8**|**`generate_batch_report` + `apply_overrides` unwired** — implementiert, getestet, aber nie aufgerufen. Gleiche Muster wie B4.|Bekannter Bug + Code-Review Rule 1|

#### Prio 3 — Stabilität & Robustheit (Code-Review Top-3)

|#|Thema|Quelle|
|---|---|---|
|**B9**|**Resource Leaks** — `fitz.open()` in `split_batch_pdf()` und `generate_report()` ohne try/finally. Bis zu 4 Handle-Leaks bei Exception.|Code-Review Rule 13|
|**B10**|**Input-Validation fehlend** — `extract_pages()` und `extract_text_via_ocr()` prüfen nicht ob Datei existiert. Opake PyMuPDF-Fehler statt klare Meldung.|Code-Review Rule 12|
|**B11**|**Batch-Error-Handling** — `_compare_pair()` ohne try/except → ein korruptes PDF crasht den gesamten Batch-Lauf.|Code-Review Rule 11|
|**B12**|**Code-Redundanz** — `_compare_pair()` dupliziert die gesamte Extraction→Compare→Report-Sequenz aus `_run_compare()`.|Code-Review Rule 2|

#### Prio 4 — Housekeeping (vor/nach Release)

|#|Thema|Quelle|
|---|---|---|
|**B13**|**Dead Code: `greet()`** in `lib.rs` — Tauri-Template-Funktion, kein Caller.|Code-Review Rule 1|
|**B14**|**Duplizierte `_region_applies_to_page`** in `ocr_extractor.py` vs. importierte Version.|Bekannter Bug|
|**B15**|**`git filter-repo`** — `tests/fixtures/TC_REAL/` noch in Remote-History.|Bekannter Bug|
|**B16**|**Test-Funktionsnamen deutsch→englisch** (165 Renames) — größter Q-Score-Hebel, aber kein Kundenimpact.|Code-Review Rule 5|
|**B17**|**Logging statt print()** auf stderr — ad-hoc Warnings ohne Level/Timestamp.|Code-Review Rule 15|
|**B18**|**Docstrings** für ~25 Helper-Funktionen.|Code-Review Rule 6|

---